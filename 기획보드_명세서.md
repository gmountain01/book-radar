# 기획 보드(Planning Board) 기능 명세서

> '리리가 도와줌' 도구에 추가할 **종합 판단 패널** 명세.
> 작성 목적: 클로드 코드가 이 문서만으로 구현에 착수할 수 있도록 함.

---

## 1. 왜 만드는가 (문제 정의)

현재 '리리가 도와줌'은 **메뉴 1개 = 검색 1회 = 결과 페이지 1개** 구조다.
- 시장 분석 메뉴 → 결과 페이지
- 키워드 분석 메뉴 → 결과 페이지
- 유튜버 분석 메뉴 → 결과 페이지(채널별 출판 적합도 점수)

각 메뉴는 데이터를 잘 **수집**하지만, 서로 **섬처럼 분리**되어 있다.
"그래서 어떤 아이템을, 어떤 저자로 가야 하는가"라는 **종합 판단**을 내리는 층이 없다.

지금은 그 종합을 사람이 손으로 한다:
여러 메뉴를 각각 검색 → 결과를 일일이 복사 → 외부(챗 등)에 모아서 → 판단.

**기획 보드는 이 "흩어진 결과를 모아 한 번에 해석하는" 그릇이다.**

---

## 2. 핵심 개념

### 2-1. 담기(Add to Board)
각 메뉴(시장/키워드/유튜버)의 결과 항목에 **"기획 보드에 담기" 버튼**을 둔다.
- 유튜버 분석에서 특정 채널 → 담기
- 시장 분석에서 특정 카테고리(공백/약점) → 담기
- 키워드 분석에서 특정 키워드(상승 트렌드) → 담기

→ **서로 다른 메뉴에서 담은 항목들이 하나의 보드에 누적**된다.
(기존 유튜버 전용 ⭐저장 목록을 "메뉴 교차 저장"으로 확장하는 개념)

### 2-2. 보드 항목의 데이터 구조
담길 때 각 항목은 출처 타입과 핵심 데이터를 함께 저장한다.

```json
{
  "id": "uuid",
  "type": "youtuber | market | keyword",
  "title": "짐코딩",
  "addedAt": "2026-05-31T...",
  "data": {
    // type별로 다름. 아래 3-2 참조
  }
}
```

### 2-3. 종합 의견 생성(Synthesize)
보드에 담긴 모든 항목을 **한 번에 클로드 API로 전송**해
"종합 의견 + 추천 아이템 + 저자 매칭 + 리스크"를 한 번에 받는다.
→ 지금까지 사람이 외부 챗으로 하던 작업을 버튼 하나로.

---

## 3. 화면/기능 명세

### 3-1. 기획 보드 패널 (신규 메뉴)
좌측 사이드바에 "기획 보드" 메뉴 추가. 클릭 시:

- **상단:** 현재 보드에 담긴 항목 수, "종합 의견 생성" 버튼, "보드 비우기" 버튼
- **중단:** 담긴 항목 리스트 (type별로 그룹핑: 유튜버 N개 / 시장 N개 / 키워드 N개)
  - 각 항목: 제목, 핵심 지표 요약, 개별 삭제 버튼
- **하단:** 종합 의견 결과 영역 (생성 후 표시)

### 3-2. type별 저장 데이터

**youtuber** (유튜버 분석에서 담기)
```json
{
  "subscribers": 48000,
  "publishFitScore": 69,        // 출판 적합도
  "contentQualityScore": 66,
  "descriptionScore": 20,        // 설명문 충실도 (집필력 지표)
  "titleScore": 20,              // 제목 전문성
  "engagementRate": 24.34,
  "commentRate": 0.04,
  "channelDescription": "...",   // 원문 (리스크 판정에 사용)
  "url": "https://..."
}
```

**market** (시장 분석에서 담기)
```json
{
  "category": "클로드 코드 / 바이브코딩 입문",
  "status": "gap | weak | strong",   // 공백/약점/강점
  "competitorCount": 9,
  "note": "경쟁서 9종, 수강생 14,390명"
}
```

**keyword** (키워드 분석에서 담기)
```json
{
  "keyword": "바이브코딩",
  "trend": "rising | stable | declining",
  "dwellDays": 42,                // 베스트셀러 체류일
  "note": "전 주제 중 최장 체류, 상승세"
}
```

### 3-3. 자동 리스크 플래그 (담길 때 자동 태깅)
유튜버 항목은 `channelDescription`을 스캔해 아래 플래그를 자동으로 붙인다.
(이 대화에서 사람이 매번 손으로 짚던 판단을 규칙화)

| 플래그 | 감지 키워드(예시) | 의미 |
|---|---|---|
| 🔴 저서 보유 | "저자", "저서", "출간", "지음" | 타사 계약 가능성 |
| 🟡 캐시카우 | "강의 문의", "클래스101", "패스트캠퍼스", "전자책", "VIP", "마스터클래스" | 출판 동기 약함 |
| 🟡 한빛 저자 | (한빛 기출간 저자 명단과 대조) | 자기잠식 위험 |
| 🔴 집필 고사 | "외부 강연 고사", "집필 집중", "정중히 고사" | 섭외 난망 |

### 3-4. 집필 적합 자동 코멘트
`descriptionScore`(설명문 충실도) 기준 자동 코멘트:
- 18~20점 → "글쓰기 검증됨 — 매뉴얼/입문서 집필 적합"
- 14~17점 → "집필력 미검증 — 샘플 원고 선확인 필요"
- ~13점 → "장문 집필 습관 약함 — 공저/구성 보강 검토"

---

## 4. 종합 의견 생성: 클로드 API 호출 명세

### 4-1. 호출 방식
보드의 모든 항목을 묶어 1회 호출. (기존 도구의 9개 API 키 로테이션 활용)

### 4-2. 시스템 프롬프트 (핵심 — "판단 렌즈"를 박아넣을 것)

```
너는 한국 IT 출판사(한빛미디어)의 베테랑 기획 편집자다.
아래 시장/키워드/유튜버 데이터를 종합해 출판 기획 판단을 내려라.

[반드시 적용할 5가지 판단 필터]
1. 한국 시장·한국 독자 우선
2. 주제 중복 회피 (외부 경쟁서 + 사내 타 팀 진행작 모두)
3. 무명 저자로도 가능해야 함 (유명 강사는 강의·전자책이 더 돈이 되어 집필 기피, 실력자는 경쟁사에 묶임)
4. 즉시 활용 가능해야 함 (따라 하면 결과 나오는 핸즈온 + 업무를 끝내주는 실무형). 경험담·에세이형 지양
5. 버전 추적 함정 회피 (도구·방법론이 6주마다 바뀜 + 한빛 신간 3~4주 지연). 원리·워크플로우 중심 선호

[판단 시 유의]
- 출판 적합도 점수만 높다고 추천하지 마라. "이 아이템에 이 채널이 맞는가"(주제 정합성)를 먼저 본다.
- 설명문 충실도 = 집필력 지표. 매뉴얼/입문서는 이 점수가 특히 중요.
- 리스크 플래그(저서 보유/캐시카우/한빛 저자/집필 고사)를 반드시 반영.
- 단정 짓지 말고, 안전 카드와 모험 카드를 구분해 제시.
```

### 4-3. 유저 메시지 (보드 데이터 주입)
```
[시장 데이터]
{market 항목들 JSON}

[키워드 데이터]
{keyword 항목들 JSON}

[유튜버/저자 후보 데이터 — 리스크 플래그 포함]
{youtuber 항목들 JSON}

위 데이터를 종합해 아래 형식(JSON)으로만 답하라. 다른 말 없이 JSON만.
```

### 4-4. 출력 스키마 (JSON 강제)
```json
{
  "summary": "한 단락 종합 의견 (시장 공백 + 키워드 + 저자를 한 흐름으로 연결)",
  "recommendedItems": [
    {
      "concept": "아이템 컨셉 한 줄",
      "rationale": "왜 유망한지 (필터 근거)",
      "fitType": "안전 | 데이터 | 모험"
    }
  ],
  "authorMatching": [
    {
      "author": "짐코딩",
      "bestFitItem": "아이템 컨셉",
      "strength": "강점",
      "risk": "리스크 (플래그 반영)",
      "verdict": "안전한 정답 | 베팅 | 보류"
    }
  ],
  "cautions": ["전체 주의사항 1", "..."]
}
```

### 4-5. 결과 렌더링
받은 JSON을 보드 하단에 카드로 렌더:
- summary → 상단 강조 박스
- recommendedItems → 아이템 카드 (fitType별 색상)
- authorMatching → 비교 표 (verdict별 색상 뱃지)
- cautions → 하단 주의 박스
- "기획서로 전달" 버튼 → 선택 항목을 기존 '저자 제안서/기획서' 탭으로 데이터 전달

---

## 5. 구현 우선순위 (단계별)

1. **STEP 1 — 담기 + 보드 누적** (필수 기반)
   - 각 메뉴 결과에 "기획 보드에 담기" 버튼
   - 보드 상태 관리(localStorage 또는 기존 저장 구조 확장), type별 그룹 렌더
2. **STEP 2 — 자동 리스크 플래그 + 집필 코멘트** (규칙 기반, API 불필요)
   - 3-3, 3-4의 키워드 규칙으로 담길 때 자동 태깅
3. **STEP 3 — 종합 의견 생성** (핵심)
   - 4번 API 호출 + JSON 파싱 + 렌더링
4. **STEP 4 — 기획서 연결**
   - 종합 결과를 기존 기획서 탭으로 넘기기

> 권고: STEP 1~2를 먼저 완성해 "모으고 자동 태깅"만 돼도 체감 효과가 크다.
> STEP 3가 R이 "부족하다"고 한 종합 판단의 핵심.

---

## 6. 참고 — 이 명세가 나온 실제 작업 흐름 (검증용 예시)

실제로 사람이 수행한 종합 과정(이 흐름을 도구가 대신하게 됨):

- 시장 분석: "클로드 코드/바이브코딩 = 공백, 경쟁서 9종"
- 키워드 분석: "바이브코딩 체류 42일 최장·상승세"
- 유튜버 분석: 짐코딩(적합69·설명문20·구독4.8만·강의보유🟡), 진한별(적합69·설명문15·구독1.1만·KAIST), 에디터몽키(적합73·품질88·설명문15)
- **사람의 종합 판단:** "클로드 코드 입문 매뉴얼북이 유망(공백+상승+즉시활용). 저자는 글쓰기 검증된 짐코딩이 안전, 진한별은 샘플 확인 후 베팅. 단 짐코딩은 강의 캐시카우 주의."

→ 기획 보드의 "종합 의견 생성"은 위 같은 판단을 자동으로 내놓는 것이 목표다.
