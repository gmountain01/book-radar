name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   # check_version.py가 전체 히스토리 필요

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      # 0) 캐시 버스팅 버전 상향 누락 검사
      #    shared/·panels/ JS·CSS 변경 후 index.html ?v= 미상향이면 실패
      - name: 캐시 버전 검사
        run: python3 scripts/check_version.py

      # 1) 전체 JS 문법 검사 (libs/ 서드파티, data/ 자동 생성 파일 제외)
      - name: 문법 검사 (node --check)
        run: |
          set -e
          find shared panels scripts -name '*.js' -not -path '*/libs/*' -print0 \
            | while IFS= read -r -d '' f; do
                node --check "$f" || { echo "문법 오류: $f"; exit 1; }
              done
          echo "문법 검사 통과"

      # 2) JSON, 교정 60건, 오탐 정책, 저자 검색, 시장 피드 회귀 테스트
      - name: 스모크 테스트
        run: node scripts/run_tests.js

  naver-key:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: 네이버 키 검증 및 실패 시 기존 파일 보존
        run: python -m unittest discover -s scripts -p test_naver_key.py -v
